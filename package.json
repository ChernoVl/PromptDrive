{
  "name": "promptdrive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "if (Test-Path dist) { Remove-Item -Recurse -Force dist }",
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "@types/node": "^20.17.47",
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}